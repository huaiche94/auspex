Generated by: codex app-server generate-json-schema (codex-cli 0.144.5, 2026-07-28)
Vendored file: codex_app_server_protocol.v2.schemas.json (the combined v2 protocol schema; the per-method files and v1 schema are regenerable from the same command and deliberately not vendored — one authoritative pin, not 3.4MB of duplicates).
Regenerate: codex app-server generate-json-schema --out <dir>

transcripts/initialize.jsonl: recorded from a live `codex app-server` initialize exchange (codex-cli 0.144.5, 2026-07-28). serverName/installationId sanitized to fixed placeholders; every other byte verbatim, including the absent jsonrpc field on responses (the server omits it) and the unsolicited remoteControl notification that follows the handshake.
