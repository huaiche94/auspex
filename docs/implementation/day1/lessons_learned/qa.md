# Lessons Learned — qa (Wave 3: qa-01, qa-08)

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| qa-01 | S (DAG: 200 LOC, 4 files) | S — matched estimate | 4 | 1 (`.github/workflows/ci.yml`) | not tracked pre-execution (DAG has no duration field) | one continuous pass, no wall-clock instrumentation in this environment | `task`/`golangci-lint` were not preinstalled in this fresh worktree; both were already available at `$(go env GOPATH)/bin` because this worktree shares the host's GOPATH with the main checkout where `foundation-09` installed them — only needed to add that dir to `PATH` for this session, not a real new dependency | `actionlint` — installed locally (`go install github.com/rhysd/actionlint/cmd/actionlint@latest`) purely to strengthen validation of the GitHub Actions YAML beyond a bare `yaml.safe_load` parse; not a repository dependency, not referenced by any tracked file | None | The DAG estimated 4 files for qa-01; 1 file (the workflow itself) was sufficient — a CI workflow that calls into an already-existing Taskfile rather than reimplementing checks inline collapses what might otherwise be a multi-file scaffold (separate lint/test/build configs) into one YAML file with a handful of jobs. This mirrors predictor's Wave-1 lesson (`predictor-03`) that DAG file-count estimates for "thin orchestration over an existing primitive" tend to run below estimate | The single most useful validation step beyond local dry-run (`task fmt/lint/build/test`) was `actionlint` — it catches GitHub Actions-specific mistakes (bad `uses:` refs, wrong `runner.os` comparisons, invalid expression syntax) that a generic YAML parser and a local shell dry-run both miss entirely, since neither actually parses the Actions schema or evaluates `${{ }}` expressions. Recommend `actionlint` be treated as a standard, cheap pre-commit check for any future `.github/workflows/*.yml` change, not just this first one |

## Cross-node observations

(To be extended once qa-08 completes in this same wave.)
